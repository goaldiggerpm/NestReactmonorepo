{
  "devDependencies": {
    "turbo": "^1.10.16"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "@nestjs/serve-static": "^4.0.0",
    "api": "^0.0.1"
  }
}
